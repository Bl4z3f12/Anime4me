<aside class="main-sidebar sidebar-dark-primary elevation-4">
    <a href="" class="brand-link">
        <img src="{{ asset('images/log.png') }}" style="opacity: 0.8; max-width: 100%; height: auto; margin: 0 auto; display: block;">
    </a>
    
    <div class="sidebar">
        <div class="user-panel mt-3 pb-3 mb-3 d-flex">
            <div class="image">
                <img src="{{ asset('images/user.png') }}" class="img-circle elevation-2" alt="user">
            </div>
            <div class="info">
                {% if app.user %}
                    <a href="#" class="d-block">Hello Mr. {{ app.user.username }}</a>
                {% else %}
                    <a href="#" class="d-block">Hello, Guest</a>
                {% endif %}
            </div>
        </div>
        
        <nav>
            <ul class="nav nav-pills nav-sidebar flex-column" data-widget="treeview" role="menu" data-accordion="false">
                <li class="nav-item">
                    <a href="{{ path('app_anime_index') }}" class="nav-link">
                        <i class="nav-icon fas fa-th"></i>
                        <p>Anime</p>
                    </a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('app_episode_index') }}" class="nav-link">
                        <i class="nav-icon fas fa-th"></i>
                        <p>Episode</p>
                    </a>
                </li>
            </ul>
        </nav>
    </div>
</aside>
